{
  "task_id": "task_0019",
  "repo_type": "validators",
  "bug_type": "string_validation",
  "bug_description": "rejects phone numbers with all same digits",
  "gold_patch": {
    "file_path": "src/validators.py",
    "old_text": "    if len(set(cleaned)) == 1:\n        return False\n    return cleaned.isdigit() and 10 <= len(cleaned) <= 15",
    "new_text": "    return cleaned.isdigit() and 10 <= len(cleaned) <= 15"
  },
  "split": "test",
  "variant_id": "val_phn_s05"
}