{
  "task_id": "task_0096",
  "repo_type": "validators",
  "bug_type": "string_validation",
  "bug_description": "accepts slash separators in date format",
  "gold_patch": {
    "file_path": "src/validators.py",
    "old_text": "    if not re.match(r\"^\\d{4}[-/]\\d{2}[-/]\\d{2}$\", date_str):\n        return False\n    try:\n        _dt.strptime(date_str.replace(\"/\", \"-\"), \"%Y-%m-%d\")",
    "new_text": "    if not re.match(r\"^\\d{4}-\\d{2}-\\d{2}$\", date_str):\n        return False\n    try:\n        _dt.strptime(date_str, \"%Y-%m-%d\")"
  },
  "split": "validation",
  "variant_id": "val_dat_s08"
}
